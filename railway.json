{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd server && yarn install && yarn start",
    "restartPolicyType": "ON_FAILURE"
  }
}
